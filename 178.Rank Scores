SELECT S.score,COUNT(S2.score) AS `rank`FROM Scores S,(SELECT DISTINCT score FROM Scores) S2
WHERE S.score <= S2.score GROUP BY S.id,S.score ORDER BY S.score DESC;

**Approach**
The "rank" of a specific score is simply the number of unique scores that are greater than or equal to it.
